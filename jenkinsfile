pipeline {
    agent any

    environment {
        TF_VERSION = '1.9.5'
        ANSIBLE_VERSION = '2.16.11' 
        SECRETS_PATH = '/var/lib/jenkins/secrets/secrets.tfvars'
    }

    stages {
        stage('Checkout Code') {
            steps {
                git branch: 'main', url: 'https://github.com/melda-20/Automation.git'
            }
        }

        stage('Terraform Init') {
            steps {
                sh '''
                cp $SECRETS_PATH ./secrets.tfvars
                terraform init
                '''
            }
        }

        stage('Terraform Plan') {
            steps {
                sh 'terraform plan -var-file=secrets.tfvars -out=tfplan'
            }
        }

        stage('Terraform Apply') {
            steps {
                sh 'terraform apply -var-file=secrets.tfvars -auto-approve'
            }
        }

        stage('Get Terraform Output') {
            steps {
                script {
                    VM_IP = sh(script: 'terraform output -raw vm_ip', returnStdout: true).trim()
                    echo "VM IP: ${VM_IP}"
                }
            }
        }

        stage('Run Ansible Playbook: apache_webserver') {
            steps {
                script {
                    // Haal de SSH-gebruiker en het wachtwoord uit secrets.tfvars
                    SSH_USER = sh(script: "grep 'ssh_user' secrets.tfvars | cut -d'=' -f2 | tr -d '\"'", returnStdout: true).trim()
                    SSH_USER_PASSWORD = sh(script: "grep 'ssh_user_password' secrets.tfvars | cut -d'=' -f2 | tr -d '\"'", returnStdout: true).trim()

                    // Voer het Ansible-playbook uit met SSH-gebruiker en wachtwoord authenticatie
                    sh """
                    ansible-playbook -i ${VM_IP}, apache_webserver.yml \
                    -u ${SSH_USER} \
                    --extra-vars "ssh_user=${SSH_USER} ssh_user_password=${SSH_USER_PASSWORD}"
                    """
                }
            }
        }
    }

    post {
        always {
            cleanWs()
        }
    }
}
